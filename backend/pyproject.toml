[project]
name = "okru-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.29.0",
    "python-dotenv>=1.0.0",
    "twitchio>=2.10.0",
    "httpx>=0.27.0",
    "scrapling>=0.2.0",
    "curl_cffi>=0.7.0",
]

[tool.setuptools.packages.find]
include = ["app*"]
