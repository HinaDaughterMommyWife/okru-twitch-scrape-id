# dependencies
node_modules/

# build output - web (astro)
web/dist/
web/.astro/

# build output - worker (rust)
worker/target/
worker/build/

# build output - rust workspace (backend, tui, db)
/target/
backend/target/
# dev SQLite (okru-backend / okru-tui debug builds)
/data/
# local / native binary + config (make install / make up)
dist/
# Oracle Linux 8 aarch64 binary + config (make build-oracle-arm)
arm/
backend/config.toml
backend/credentials.json

# environment & secrets
.env
.env.*
!.env.example
!.dev.vars.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# os
.DS_Store
Thumbs.db

# editors
.idea/
.vscode/
*.swp
*.swo

# wrangler
.wrangler/
.dev.vars
worker-configuration.d.ts

# python (legacy leftovers)
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# pm2 logs
logs/

# agents
.agents/
skills-lock.json

